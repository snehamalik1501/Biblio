# Copy this file to .env and fill in your MySQL credentials.
# The app will create the "shelfspace" database and tables automatically
# on first run if they don't already exist (the DB user needs CREATE privileges).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=shelfspace
PORT=4000
